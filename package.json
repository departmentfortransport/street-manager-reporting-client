{
  "name": "street-manager-reporting-client",
  "version": "1.0.0",
  "description": "The client used to communicate with the street-manager-reporting-api",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/departmentfortransport/street-manager-reporting-client.git"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "lint": "tslint --project tslint.json",
    "clean": "rimraf dist",
    "build": "npm run clean && npm run test && tsc",
    "test": "npm run lint"
  },
  "license": "MIT",
  "dependencies": {
    "axios": "^0.19.2",
    "http-status-codes": "^1.4.0",
    "qs": "^6.9.1"
  },
  "devDependencies": {
    "@types/node": "^12.12.26",
    "rimraf": "^2.7.1",
    "tslint": "^5.20.1",
    "typescript": "^3.7.5"
  }
}
